/**
 * 
 * Things to do today:

NOTE: Checkout the update info controller before proceeding with the post.

NOTE: Take validation into consideration.

-> Get user information, update user information and update password: How:
    a> Setup route to get user information in the update info route.
    b> Display the received information.
    c> Make another component to change password.
    d> Put all the axios request on the page.
-> Make reservation via user.
-> Update reservation.
-> feedback post way.
-> Format the reservation table (Orders menu) to make the time look formatted.
-> Orders table should be split in two (active and history one)
 */